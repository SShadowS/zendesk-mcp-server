ZENDESK_SUBDOMAIN=your-subdomain
    ZENDESK_EMAIL=your-email@example.com
    ZENDESK_API_TOKEN=your-api-token

    # Retry Configuration (Optional)
    ZENDESK_MAX_RETRIES=3              # Maximum retry attempts for failed requests (default: 3)
    ZENDESK_RETRY_DELAY=1000           # Initial retry delay in milliseconds (default: 1000)
    ZENDESK_RETRY_MAX_DELAY=30000      # Maximum retry delay in milliseconds (default: 30000)
    ZENDESK_DEBUG=false                # Enable debug logging for API requests (default: false)
