# Zendesk MCP Server Configuration
# Copy this file to .env and fill in your actual values

# Required: Zendesk Authentication
# Your Zendesk subdomain (e.g., "mycompany" for mycompany.zendesk.com)
ZENDESK_SUBDOMAIN=your-subdomain-here

# Email address of the Zendesk user
ZENDESK_EMAIL=your-email@example.com

# API token for authentication
# Generate from: Zendesk Admin → API → Settings
ZENDESK_API_TOKEN=your-api-token-here

# Optional: Anthropic API Key
# Required only for the analyze_ticket_images feature
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Note: Never commit your actual .env file to version control!
# The .env file is already in .gitignore for your protection.